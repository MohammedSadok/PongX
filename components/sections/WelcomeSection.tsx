"use client";

const WelcomeSection: React.FC = () => {
  return (
    <section id="welcome" className="container mx-auto py-16 px-4 md:px-8 flex flex-col items-center justify-center text-center h-full">
      {/* Using a simple heading for the title */}
      {/* <h1 className="text-5xl md:text-7xl font-bold mb-4 text-primary animate-pulse">
        Prompting Is All You Need
      </h1> */}
      {/* You could potentially render the pixel art here if refactored */}
    </section>
  );
};

export default WelcomeSection;
